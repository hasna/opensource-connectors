# Web Placeholder

The `apps/web` directory reserves space for a future management UI for the Google Drive connector. For the alpha milestone, the UI is not implemented. When front-end work begins, scaffold the project with Vite + React and expose local development on `https://googledrive.connect`.
